# Generic repositories to clone, one per line:
# URL<TAB>TARGET
#
# Example:
# git@github.com:owner/repo.git	~/dev/repos/repo
#
# worklog deliberately does not belong here. It is an ordinary checkout at
# ~/dev/repos/worklog, but it needs `scripts/setup.sh` run after cloning; a
# plain repos.txt clone would skip that. steps/70-worklog.sh does both.
#
# Engineering System is a public tooling checkout. Private knowledge lives in
# ~/.local/share/engineering-system-data and must never be committed here.
# Step 75 runs setup after Worklog and before central settings reconciliation.
https://github.com/jf-Lindberg/engineering-system.git	~/dev/repos/engineering-system
