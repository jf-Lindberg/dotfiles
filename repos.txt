# Generic repositories to clone, one per line:
# URL<TAB>TARGET
#
# Example:
# git@github.com:owner/repo.git	~/dev/repos/repo
#
# worklog deliberately does not belong here; steps/70-worklog.sh installs it.
#
# Add engineering-system here once it has a cloneable remote. Step 75 runs its
# setup after Worklog and before the central Claude settings reconciliation:
# <engineering-system-remote>	~/dev/repos/engineering-system
